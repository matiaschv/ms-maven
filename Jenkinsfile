pipeline {
    agent any
    
    stages {
        stage('initial'){
            steps {
                sh'''
                echo "PATH = ${PATH}"
                echo "M_HOME = ${M_HOME}"
            }
        }
    }
}
